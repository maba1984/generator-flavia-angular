language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-flavia-angular' ]; then cd .. && eval "mv $currentfolder generator-flavia-angular" && cd generator-flavia-angular; fi

